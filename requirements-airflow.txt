# Dépendances installées dans l'image Airflow custom
# (pyspark exclu : Spark tourne dans son propre container)

# Ingestion + Snowflake load
requests==2.31.0
pandas==2.2.2
pyarrow==16.0.0
boto3==1.34.84
snowflake-connector-python==3.9.1
cryptography>=41.0.0

# DBT
dbt-core==1.8.3
dbt-snowflake==1.8.3
